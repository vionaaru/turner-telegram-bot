# 🤖 Telegram Bot: Turner CRM (Токарные работы)
[![Website](https://img.shields.io/website?label=akulov-dev.ru&style=for-the-badge&url=https%3A%2F%2Fakulov-dev.ru)](https://akulov-dev.ru)  [![Developer](https://img.shields.io/badge/Developer-Sergey%20Akulov-blue)](https://github.com/serg-akulov) [![Profile Views](https://komarev.com/ghpvc/?username=serg-akulov-turner-bot&label=Project%20Views&color=0e75b6&style=flat)](https://github.com/serg-akulov)

<img width="1166" height="735" alt="11111111111111111111111111111111" src="https://github.com/user-attachments/assets/6d8b5782-3fc8-444b-a128-532281845ed8" />

---
**🎉 ОБНОВЛЕНИЕ!** Теперь с современной веб-админкой на React + Ant Design!

Простой и мощный бот для приема заказов на токарные/фрезерные работы с удобной админ-панелью. Клиент проходит опрос (фото, тип работы, размеры), а вы получаете структурированный заказ.

### ✨ Новые возможности админки:
- 🎨 **Современный интерфейс** на Ant Design
- 📊 **Дашборд** со статистикой заказов
- 🔍 **Удобная таблица** заказов с фильтрами
- ⚙️ **Конструктор бота** - настройка всех текстов
- 🖼️ **Просмотр фото** через Telegram API
- 📱 **Адаптивный дизайн** для всех устройств

### ✨ Возможности
*   📸 **Прием фото и чертежей** (файлы сохраняются в Telegram, не занимают место на сервере).
*   🏗 **Конструктор опроса** (можно менять тексты вопросов и кнопок через админку).
*   📋 **CRM-система** (PHP) для управления статусами (Новый -> В работе -> Готов).
*   💬 **Чат с клиентом** (ответы на сообщения бота пересылаются пользователю).
*   🚀 **Легкая установка** (автоматический bash-скрипт).

---

<img width="1638" height="1312" alt="333333333333333333" src="https://github.com/user-attachments/assets/630d607f-e51a-496a-8327-0da0e4a04499" />
<img width="1636" height="582" alt="222222222222222222222222" src="https://github.com/user-attachments/assets/f745dfb8-a063-4c3b-867d-98164ca6b9eb" />
<img width="1333" height="760" alt="44444444444444" src="https://github.com/user-attachments/assets/45f0ded2-cc06-470d-917a-1e0f96e4b8a0" />

---
### 🛠 Требования
*   VPS/VDS с **Ubuntu 20.04** или **22.04** (можно и Debian).
*   Установленный **MySQL** (или MariaDB) и **Web-сервер** (Apache/Nginx + PHP) для админки.

---

### 🚀 Установка и запуск

#### Вариант 1: Docker Compose (Рекомендуется)
Запускает всё автоматически: бот, базу данных, API и веб-интерфейс.

1. **Установите Docker и Docker Compose**

2. **Клонируйте репозиторий:**
   ```bash
   git clone https://github.com/serg-akulov/turner-telegram-bot.git
   cd turner-telegram-bot
   ```

3. **Создайте .env файл:**
   ```bash
   # Создайте файл .env со следующим содержимым:
   ```

   ```env
   # Токен Telegram бота (получить у @BotFather)
   BOT_TOKEN=your_bot_token_here

   # Настройки базы данных
   DB_HOST=mysql
   DB_NAME=turner_db
   DB_USER=turner_user
   DB_PASS=turner_password
   DB_ROOT_PASSWORD=root_password

   # Пароли для админки
   ADMIN_PANEL_PASSWORD=admin123
   BOT_ADMIN_PASSWORD=botadmin123

   # Секретный ключ для JWT (измените на случайный)
   SECRET_KEY=your-super-secret-key-change-this-in-production
   ```

4. **Запустите всё одной командой:**
   ```bash
   docker compose up -d
   ```

5. **Доступ к сервисам:**
   - 🌐 **Админка:** http://localhost:3000 (логин: admin123)
   - 🤖 **Бот API:** работает автоматически
   - 📊 **API документация:** http://localhost:8000/docs

#### Вариант 2: Локальная установка (для разработки)
Если хотите разрабатывать или запускаете на VPS без Docker.

1. **Установите зависимости:**
   ```bash
   # Python зависимости
   pip install -r requirements.txt

   # Node.js для фронтенда (если нужно разрабатывать)
   cd frontend && npm install
   ```

2. **Настройте базу данных MySQL** (локально или в Docker)

3. **Запустите сервисы:**
   ```bash
   # Бэкенд API
   cd backend && python main.py

   # Фронтенд (для разработки)
   cd frontend && npm start

   # Бот
   python bot.py
   ```

---


### 🌐 Админка (React + Ant Design)

**В Docker:** Админка автоматически доступна на http://localhost:3000
**Локально:** Запустите `cd frontend && npm start`

#### 🔐 Вход в админку
- **Логин:** не требуется
- **Пароль:** admin123 (или тот, что указан в .env)

#### 📋 Возможности новой админки

##### 🏠 Дашборд
- Статистика заказов (всего, новых, активных)
- Быстрые действия

##### 📦 Управление заказами
- Таблица всех заказов с пагинацией
- Фильтры по статусу
- Просмотр деталей заказа
- Изменение статуса
- Просмотр фото через Telegram API
- Внутренние заметки

##### ⚙️ Настройки бота
- **Конструктор текстов** - настройка всех сообщений бота
- **Системные настройки** - поведение бота, обязательность фото
- Изменения применяются после перезапуска бота

#### 🎨 Дизайн и UX
- Адаптивный интерфейс для мобильных устройств
- Темная/светлая тема (Ant Design)
- Интуитивная навигация
- Быстрый поиск и фильтры

---

### 🏗️ Архитектура проекта

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   React Frontend│    │   FastAPI Backend│    │   Telegram Bot  │
│   (Ant Design)  │◄──►│   (Python)       │◄──►│   (Aiogram)     │
│   Port: 3000    │    │   Port: 8000     │    │                 │
└─────────────────┘    └─────────────────┘    └─────────────────┘
         │                       │                       │
         └───────────────────────┼───────────────────────┘
                                 ▼
                    ┌─────────────────┐
                    │   MySQL Database│
                    │   (Orders, Config)│
                    └─────────────────┘
```

#### 📁 Структура проекта
```
├── bot.py              # Основной бот (Aiogram)
├── database.py         # Работа с MySQL
├── config.py          # Конфигурация
├── backend/           # FastAPI бэкенд
│   ├── main.py
│   ├── routers/
│   └── requirements.txt
├── frontend/          # React админка
│   ├── src/
│   ├── package.json
│   └── Dockerfile
├── docker-compose.yml # Docker оркестрация
└── schema.sql         # Структура БД
```

### 🤖 Как управлять ботом

#### 1. Через новую Web-админку (рекомендуется)
- **Заказы:** Просмотр, фильтрация, изменение статусов с уведомлениями клиентам
- **Настройки:** Конструктор всех текстов и параметров бота
- **Статистика:** Дашборд с метриками

#### 2. Через Telegram (для администратора)
1. **Авторизация:** `/iamadmin ВАШ_ЦИФРОВОЙ_ПАРОЛЬ`
2. **Уведомления:** Бот автоматически присылает новые заказы
3. **Ответы:** Reply на сообщения для ответа клиентам

---

### 🛠 Полезные команды

#### Docker команды
```bash
# Запуск всех сервисов
docker compose up -d

# Остановка всех сервисов
docker compose down

# Просмотр логов
docker compose logs -f

# Пересборка после изменений
docker compose build --no-cache

# Вход в контейнер
docker compose exec backend bash
```

# Перезапуск бота (после изменения настроек)
docker compose restart bot

# Просмотр логов бота
docker compose logs -f bot

# Просмотр логов API
docker compose logs -f backend

#### Работа с базой данных
```bash
# Вход в MySQL контейнер
docker compose exec mysql mysql -u turner_user -p turner_db

# Бэкап базы
docker compose exec mysql mysqldump -u turner_user -p turner_db > backup.sql
```

---

### 📂 Структура проекта
*   `bot.py` — Основной код бота (Aiogram).
*   `admin.php` — Веб-интерфейс (CRM).
*   `schema.sql` — Структура базы данных.
*   `install.sh` — Автоматический установщик.
*   `php_config.php` — Генерируется автоматически, хранит пароли для админки.

---
👨‍💻 Автор: Сергей Акулов

Инди-разработчик и Токарь.

🛠️ Больше полезного софта на моем сайте: [akulov-dev.ru](https://akulov-dev.ru)

Разработка и поддержка.
GitHub: [@serg-akulov](https://github.com/serg-akulov)

## Благодарности

Огромное спасибо [@dedkovd](https://github.com/dedkovd) за реализацию Docker-контейнеризации — это сделало проект значительно удобнее для развертывания.

Copyright © 2025. Released under the MIT License.
