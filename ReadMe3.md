# 🤖 Turner CRM: Умный бот для приема заказов

Добро пожаловать в **Turner CRM** — готовое решение для токарей, фрезеровщиков и мастеров, которые хотят автоматизировать прием заказов через Telegram и управлять ими в современной админ-панели.

---

## 🔥 Что умеет этот проект?

### 📱 Для клиента (Telegram Бот)
*   **Удобный опрос**: Бот шаг за шагом собирает всю нужную информацию (Тип работы, размеры, условия эксплуатации, срочность).
*   **Фото и чертежи**: Клиент может прислать любое количество фото.
*   **Уведомления**: Как только вы смените статус заказа в админке, клиент получит вежливое уведомление от бота.
*   **Прямая связь**: Вы можете отвечать клиенту прямо из бота (через Reply), и он получит ваше сообщение.

### 💻 Для мастера (Web Админка)
*   **Дашборд**: Быстрая статистика по заказам (Всего / Новые / В работе).
*   **Управление заказами**: Полноценная таблица с фильтрами по статусам.
*   **Детали заказа**: Просмотр всех данных клиента, включая фото (загружаются напрямую из Telegram).
*   **Конструктор бота**: Меняйте любые тексты сообщений и кнопок бота прямо в браузере без правки кода!
*   **Внутренние заметки**: Оставляйте комментарии к заказам, которые видит только мастер.

---

## 🚀 Быстрый старт (Docker)

Проект полностью контейнеризирован. Вам не нужно устанавливать Python или Node.js на сервер.

1. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/serg-akulov/turner-telegram-bot.git
   cd turner-telegram-bot
   ```

2. **Настройте окружение**:
   Скопируйте пример конфига и впишите свой `BOT_TOKEN`:
   ```bash
   cp .env.example .env
   nano .env
   ```

3. **Запустите проект**:
   ```bash
   docker compose up -d
   ```

---

## 📦 Обновление и миграция данных

### Если вы уже используете бота без Docker
Проект полностью сохраняет обратную совместимость. Вы можете просто обновить код через `git pull` и продолжать запускать бота вручную (`python3 bot.py`).

### Как перенести данные в Docker (Миграция)
Если вы хотите перейти на Docker, сохранив старые заказы:

1. **Сделайте дамп вашей текущей базы данных**:
   ```bash
   mysqldump -u <ВАШ_СТАРЫЙ_DB_USER> -p <ВАША_СТАРАЯ_DB_NAME> > dump.sql
   ```
   *Здесь используйте имя пользователя и имя базы данных из вашего старой конфигурации (те же, что были прописаны в `.env` до обновления).*

2. **Запустите Docker-контейнеры**:
   ```bash
   docker compose up -d
   ```

3. **Импортируйте данные в контейнер**:
   ```bash
   docker exec -i turner-mysql mysql -u turner_user -pturner_password turner_db < dump.sql
   ```
   *В этой команде:*
   - `turner-mysql` — это имя контейнера с базой данных (задано в `docker-compose.yml`).
   - `-u turner_user` — имя пользователя БД внутри Docker.
   - `-pturner_password` — пароль пользователя БД (пишется слитно с `-p`).
   - `turner_db` — имя базы данных внутри Docker.
   *Все эти значения (`user`, `password`, `dbname`) мы берем из вашего файла `.env`.*

---

## 🔑 Первые шаги после запуска

### 1. Как стать админом в боте
Чтобы бот присылал вам уведомления о новых заказах:
1. Зайдите в своего бота в Telegram.
2. Введите команду: `/iamadmin ВАШ_ПАРОЛЬ` (пароль берется из `.env`, переменная `BOT_ADMIN_PASSWORD`).
3. Теперь вы будете получать карточку каждого нового заказа!

### 2. Вход в веб-админку
1. Откройте в браузере: `http://ваш-ip:45554`
2. Используйте пароль из `.env` (`ADMIN_PANEL_PASSWORD`).
3. По умолчанию логин не требуется, только пароль.

---

## 🛠 Архитектура
*   **Frontend**: React + Ant Design (Порт 45554)
*   **Backend**: FastAPI (Порт 45556)
*   **Bot**: Python (Aiogram 3)
*   **DB**: MySQL 8.0

---

## 📖 FAQ

**В: Где хранятся фото заказов?**
О: Фото хранятся на серверах Telegram. Бэкенд автоматически запрашивает временные ссылки через Telegram API (`getFile`) для отображения в админке. Это экономит место на вашем сервере и гарантирует актуальность ссылок.

**В: Работают ли уведомления при смене статуса?**
О: Да! Система автоматически отправляет сообщение клиенту в Telegram, когда вы выбираете новый статус в админ-панели.

**В: Как отвечать клиенту?**
О: Просто сделайте "Ответ" (Reply) на сообщение бота с заказом в Telegram. Ваш ответ уйдет клиенту.

---
👨‍💻 Автор: **Сергей Акулов**
Сайт: [akulov-dev.ru](https://akulov-dev.ru)
GitHub: [@serg-akulov](https://github.com/serg-akulov)
